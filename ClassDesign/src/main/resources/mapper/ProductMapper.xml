<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yl.cd.mapper.ProductMapper">
<!-- 		<resultMap type="paginationBean" id="paginationBeanMap">
		<collection property="rows" column="{pageSize=pageSize ,currPage=currPage}" select="getProduct"/>
	</resultMap>
	
	<select id="getAllProduct" parameterType="paginationBean" resultMap="paginationBeanMap">
		select count(1) total,ceil(count(1)/${pageSize}) totalPage,${pageSize} pageSize,${currPage} currPage from cproduct
	</select>
	<select id="getProduct" resultType="Cproduct">
		select * from
		(select m.*,rownum rn from
		(select * from cproduct order by 1) m where ${currPage}*${pageSize}>=rownum) where rn>(${currPage}-1)*${pageSize}
	</select> -->
	<select id="getProductByName" parameterType="map" resultType="Cproduct">
		select * from (select t.*,rownum rownu from cproduct t where
		rownum&lt;=#{currPage}*#{pageSize} and 1=1 and cpstatus='normal'
		<choose>
			<when test="cproductname!=null and cproductname!=''">
				and cproductname=#{cproductname,jdbcType=VARCHAR}
			</when>
		</choose>
		)tt where tt.rownu>(#{currPage}-1)*#{pageSize}
	</select>	
	
	<select id="getProductTotalAndTotalPage" parameterType="map"
		resultType="PaginationBean">
		select count(1) total,ceil(count(1)/${pageSize})
		totalPage,${pageSize}
		pageSize,${currPage} currPage from cproduct where 1=1 and cpstatus='normal'
		<choose>
			<when test="cproductname!=null and cproductname!=''">
				and cproductname=#{cproductname,jdbcType=VARCHAR}
			</when>
		</choose>
	</select>

</mapper>